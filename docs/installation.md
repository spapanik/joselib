# Installation

The easiest way is to use [poetry] to manage your dependencies
and add _joselib_ to them.

```toml
[tool.poetry.dependencies]
joselib = "^0.1"
```

[poetry]: https://python-poetry.org/
